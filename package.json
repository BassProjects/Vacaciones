{
  "name": "electropolis-vacaciones",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "node --experimental-vm-modules --test tests/*.test.mjs",
    "check:release": "npm test && npm run build && npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "next": "14.2.35",
    "nodemailer": "^10.0.10",
    "pdf-parse": "^1.1.1",
    "pg": "^8.11.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  }
}
